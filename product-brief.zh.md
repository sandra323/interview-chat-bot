# 产品简报（Product Brief）

## 1. 产品概述

- **产品是什么：** 面向**前端开发者**的 Web 端 AI 模拟面试应用。用户注册账号后，配置面试（方向 + 难度），通过实时多轮问答与 AI 面试官对练，之后可回看已保存的完整对话记录。
- **目标用户：** 正在求职或提升技能的前端开发者，用于准备技术面试（初级至高级）。
- **解决的核心问题：** 技术面试练习成本高、难预约、或过于被动。用户需要按需、聚焦前端、可互动、可暂停/恢复/回看的模拟面试。
- **为什么现在做：** 大语言模型能以较低成本实现自适应追问；WebSocket 支持实时对话；面试备考是高意图、高频复购场景。

**明确假设：**
- Web 应用（桌面优先，移动端自适应）
- **仅前端工程师方向** — v1 不做后端面试
- **v1 不做评估报告** — 仅保存对话记录与会话历史；评分/报告推迟至 v2
- 支持自助**注册** + 登录
- 界面以中文为主，技术术语可用英文
- 后端对接单一 LLM 提供商（待定）
- 滑块验证码仅在登录时使用（注册及其他操作不需要）

---

## 2. 目标

- **主要目标：** 让已注册用户完成一次完整的前端模拟面试——从配置、多轮问答到结束会话——并可恢复中断的会话或回看对话记录。
- **次要目标：**
  - 按难度（初级 / 中级 / 高级）校准提问深度
  - 完整保存会话历史，便于日后回顾
  - 面试过程中保持稳定的实时聊天体验
- **成功指标 / 可量化结果：**
  - 上线 7 天内，≥80% 已开始的面试达到 `completed` 状态
  - WebSocket 会话可用率 ≥95%（活跃面试中无不可恢复的断连）
  - 从点击「开始面试」到 AI 发出第一题的中位时间 ≤3 秒
  - ≥60% 完成一次面试的用户在 14 天内开始第二次
  - 注册 → 首次完成面试的转化率在 7 天内 ≥40%

---

## 3. 目标用户

- **主要用户：** 正在求职或技能提升的前端开发者。
- **用户特征：**
  - 熟悉 Vue、React、JavaScript 或浏览器原理
  - 需要结构化的模拟面试，而非泛用 AI 聊天
  - 重视对话记录保存与会话连续性
  - 使用桌面浏览器进行专注练习
- **核心使用场景：**
  1. **面试前冲刺：** 用户选择方向 + 难度，在真实公司面试前进行 20–40 分钟模拟练习。
  2. **薄弱项训练：** 针对特定方向（如性能优化）重复进行中级/高级难度练习。
  3. **对话回顾：** 浏览历史会话，重读问答内容，反思作答质量。

---

## 4. 问题陈述

- **当前痛点：**
  - 真人模拟面试昂贵且难预约
  - 静态题库无法根据薄弱回答自适应追问
  - 泛用 AI 聊天缺乏面试结构与持久化历史
  - 中途退出会丢失上下文与进度
- **现有替代方案：** LeetCode（偏刷题）、面试书籍、付费模拟平台、临时 ChatGPT 提问、同伴互练。
- **现有方案不足：**
  - 缺少简洁的端到端流程：注册 → 配置 → 实时面试 → 持久化 → 回看
  - 缺少面向前端、按难度校准的面试预设
  - 开箱即用的实时、有序、可重连对话能力不足
  - v1 聚焦**练习 + 对话记录**；结构化评分可后续迭代

---

## 5. MVP 范围

### 范围内（In Scope）

- **注册：** 支持**邮箱、手机号或用户名** + 密码自助注册；基础校验（标识唯一性、密码规则）
- **认证：** 登录/登出；登录时滑块验证码；持久化登录态（Token/Cookie）
- **面试配置：** 固定岗位 — **前端开发工程师**；用户选择：
  - **面试方向/主题：** Vue、React、JavaScript、性能优化、浏览器原理等（可配置列表）
  - **难度：** 初级、中级、高级
- **实时 AI 面试：** WebSocket 双向通信；AI 主动发起首题；基于上下文多轮追问；实时消息列表 UI
- **会话持久化：** 保存进行中会话元数据 + 完整对话（AI 提问、用户回答、AI 追问）；支持重新进入后恢复
- **会话结束：** 用户主动结束；会话标记为 `completed`（不生成报告）
- **历史记录：** 列出历史面试；查看任意会话的完整对话
- **WebSocket 可靠性：** 心跳检测；**指数退避 + 随机抖动**自动重连（**最多 5 次**）；有序消息投递（服务端序列号）

### 范围外（Out of Scope）

- **后端开发工程师面试**（Java、Spring 等）
- **AI 评估报告**（综合评分、能力维度分析、改进建议）— **v2**
- 语音/视频面试
- 代码编辑器 / 现场 coding / 白板
- 真人面试官或同伴匹配
- 付费、订阅、积分
- 管理后台、内容审核控制台
- 邮箱/短信验证（除非后续需要）
- 移动端原生 App
- 社交分享、排行榜、 gamification
- 自定义 JD 或公司专属题库
- 实时监考/防作弊
- 团队账号、招聘方功能
- 导出 PDF
- 岗位/角色选择器（v1 固定为前端）

---

## 6. 用户流程

**主流程：注册并完成一次模拟面试**

1. **入口：** 未登录用户进入登录页；提供注册页链接。
2. **注册：** 用户输入**邮箱、手机号或用户名** + 密码（+ 确认密码）→ 校验通过 → 创建账号 → 跳转登录页或自动登录。
3. **登录：** 用户输入凭证 → 完成滑块验证码 → 获得持久化登录态 → 进入控制台/首页。
4. **创建面试：** 用户点击「新建面试」→ 选择**方向/主题** → 选择**难度**（初级 / 中级 / 高级）→ 确认。（岗位默认为前端工程师。）
5. **开始：** 用户点击「开始面试」→ 系统创建会话 → 进入面试间 → 建立 WebSocket 连接。
6. **实时面试：** AI 发送开场问题 → 用户输入回答并发送 → AI 基于上下文追问 → 循环直至：
   - 用户点击「结束面试」，**或**
   - 系统达到可配置的轮次/时长上限（如 8–12 轮或 30 分钟）— *MVP 假设*。
7. **决策点（断连）：** 连接断开时，客户端以**指数退避 + 随机抖动**策略自动重连，**最多 5 次**；成功后拉取会话状态 + 消息历史并继续。5 次均失败后展示断开状态，并提供手动重试入口。
8. **完成：** 结束后会话状态 → `completed` → 展示简单完成提示（无报告）→ 可返回控制台或查看对话记录。
9. **会话后：** 用户打开历史记录 → 选择某次会话 → 查看完整问答对话。

**次要流程：恢复中断的面试**

1. 用户登录 → 控制台/历史列表显示「进行中」会话。
2. 用户打开该会话 → WebSocket 重连 → 恢复对话记录 → 继续面试。

---

## 7. 功能需求

### 注册
- FR-1：用户可使用**邮箱、手机号或用户名** + 密码注册（至少填写一种标识）。
- FR-2：注册时校验必填项、密码确认一致、最低密码规则，以及手机号格式（如填写）。
- FR-3：邮箱、手机号或用户名重复时拒绝注册并给出明确错误提示。
- FR-4：注册成功后创建用户账号，并跳转登录页或已登录首页。

### 认证
- FR-5：用户可使用任意已注册标识（邮箱、手机号或用户名）+ 密码登录。
- FR-6：登录前须通过滑块验证码校验。
- FR-7：登录态在浏览器刷新后保持（带过期时间的 Token 或 Session Cookie）。
- FR-8：用户可登出；登出后清除客户端会话，访问受保护页面需重新登录。

### 面试配置
- FR-9：所有面试固定为**前端开发工程师**（无岗位选择 UI）。
- FR-10：用户从预设列表中选择一个方向/主题（至少包含：Vue、React、JavaScript、性能优化、浏览器原理）。
- FR-11：用户选择难度：**初级**、**中级**或**高级**。
- FR-12：开始面试时创建唯一会话 ID 并跳转至面试间。

### 实时对话
- FR-13：面试间建立与当前用户及会话绑定的 WebSocket 连接。
- FR-14：会话开始后，服务端/AI 自动发送第一道面试题。
- FR-15：用户可发送文字回答；每条消息在 ACK 前或同时持久化。
- FR-16：AI 基于同一会话内历史上下文生成追问。
- FR-17：聊天 UI 按时间顺序展示消息，含角色标识（AI / 用户）与时间戳。
- FR-18：用户可随时手动结束面试。

### 会话与历史
- FR-19：系统保存会话配置（岗位固定为 FE、方向、难度、状态、时间戳）。
- FR-20：系统保存每条消息（角色、内容、序列号、创建时间）。
- FR-21：进行中的会话出现在用户列表中，可恢复继续。
- FR-22：已完成的会话在面试间视图中为只读。
- FR-23：结束面试时会话状态设为 `completed`，不触发报告生成。

### WebSocket
- FR-24：服务端以固定间隔（如 30 秒）发送心跳（ping/pong）；客户端将心跳超时视为断连。
- FR-25：会话活跃期间，客户端以**指数退避 + 随机抖动**策略自动重连，每次断连事件**最多重试 5 次**；退避上限如 30 秒（抖动前）。
- FR-25a：5 次重连均失败后，客户端停止自动重试，展示断开状态，并提供「手动重连」操作。
- FR-26：消息携带单调递增序列号；客户端按序渲染；忽略重复消息。
- FR-27：重连后，客户端拉取自上次 ACK 序列号以来的遗漏消息。

### 历史记录
- FR-28：用户可分页查看历史会话列表（日期、方向、难度、状态）。
- FR-29：用户可打开任意会话查看完整问答对话。

---

## 8. 非功能性要求

- **性能：** WebSocket 就绪后，首题 p95 ≤3 秒；消息发送 ACK p95 ≤500 毫秒。
- **可靠性：** 正常断连/重连不丢消息；服务端重启后可从数据库恢复会话状态。
- **易用性：** 面试间无需培训即可使用；连接状态清晰可见（已连接 / 重连中 / 已断开 / 重连失败）；重连期间禁用发送；展示重连进度（如「重连中 2/5」）。
- **响应式：** 布局适配 ≥1280px 桌面，平板（~768px）可降级使用；输入框固定在底部。
- **安全/隐私：** 仅 HTTPS/WSS；所有会话 API 需认证；用户只能访问自己的会话；登录验证码；注册/登录/发消息限流；密码服务端哈希存储；LLM API Key 仅存服务端。
- **可维护性：** 方向与难度列表可配置（环境变量或 DB 种子）；Prompt 模板按方向 + 难度版本化；WebSocket 与 REST 共用同一套会话/消息模型。

---

## 9. 风险与待确认问题

### 产品风险
- AI 提问可能与所选方向不够贴合 → 按方向 + 难度设计结构化 System Prompt 缓解。
- 用户不知何时结束 → 提供明显的「结束面试」按钮 + N 轮后软性提示。
- v1 无报告可能让用户感觉不完整 → 在 UI 文案中设定预期；v1 价值在于对话练习与记录回顾。

### 技术风险
- LLM 延迟影响实时感 → 流式输出到 UI；展示「正在输入」状态。
- 负载均衡后 WebSocket 多实例扩展 → 粘性会话或共享 Pub/Sub。
- 长对话超出 LLM 上下文 → 早期轮次摘要后用于追问；v1 限制会话长度。

### 体验风险
- 重连可能导致消息重复 → 严格按序列号去重 + 「已重连」提示。
- 最多 5 次重连后用户可能卡在中途 → 展示明确的失败状态 + 手动重试；会话仍可通过刷新页面或重新进入恢复。
- 滑块验证码增加摩擦 → 仅登录时使用，注册不需要。
- 空历史、注册校验失败等需有清晰文案。

### 依赖风险
- LLM 提供商的可用性、定价与内容政策
- 验证码 SDK 集成
- 部署环境的 WebSocket 代理超时

### 待确认问题 / 未明假设
| 事项 | MVP 假设 | 是否需要确认？ |
|------|---------|--------------|
| 注册后是否自动登录 | 跳转至登录页 | 可选 |
| 面试时长 | 约 8–12 轮问答或 30 分钟上限 | 是 |
| 面试语言 | 中文提问，可夹杂英文术语 | 是 |
| 流式响应 | AI 文字流式输出到 UI | 建议 |
| 方向列表 | 保留 Vue/React/JS 等子主题 | 是 — 或仅保留难度？ |

---

## 10. 验收标准

- [ ] 用户可使用邮箱、手机号或用户名 + 密码注册；重复的邮箱、手机号或用户名被拒绝。
- [ ] 用户可使用任意已注册标识（邮箱、手机号或用户名）+ 密码登录。
- [ ] 注册后可登录；登录须通过有效滑块验证码。
- [ ] 登录后在页面刷新前保持登录态，直至登出或过期。
- [ ] 用户可登出；未重新登录无法访问受保护页面。
- [ ] 面试配置**无后端岗位选项**；仅前端方向。
- [ ] 用户可选择方向/主题及**初级 / 中级 / 高级**难度，并进入面试间且 WebSocket 正常连接。
- [ ] 连接建立后 3 秒内（staging 环境 p95），AI 自动发送第一题。
- [ ] 用户消息在 UI 中展示，且刷新页面后仍存在。
- [ ] AI 追问能引用用户先前回答（每个难度手动抽检 3 个样本会话）。
- [ ] 模拟断网 ≤30 秒后重连，对话完整无缺失，UI 无重复消息。
- [ ] 用户可中途离开、重新登录、打开同一会话并继续对话。
- [ ] 用户可结束面试；会话状态变为 `completed`。
- [ ] **结束后不生成、不展示评估报告** — 仅完成提示 + 对话记录访问。
- [ ] 历史列表展示会话日期、方向、难度、状态（v1 无评分列）。
- [ ] 打开历史会话可查看与线上一致的完整问答对话。
- [ ] WebSocket 心跳与自动重连（指数退避 + 随机抖动，最多 5 次）可通过网络工具验证。
- [ ] 5 次重连均失败后，UI 展示断开/失败状态及手动重试入口；不会出现无限静默重试。
- [ ] 消息严格按服务端序列号 chronological 顺序展示。

---

*文档版本：MVP v1.2 · 仅前端 · 无评估报告 · 注册支持邮箱/手机号/用户名 · WebSocket 重连：退避+抖动，最多 5 次 · 供设计、迭代拆分及 AI 辅助开发使用。*
